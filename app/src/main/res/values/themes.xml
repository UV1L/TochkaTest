<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TochkaTest" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_gray</item>
        <item name="colorPrimaryDark">#919191</item>
        <item name="colorAccent">@color/very_dark_blue</item>
        <item name="android:textColor">#000000</item>
        <item name="cardBackgroundColor">#3DFFC0</item>
        <!-- Customize your theme here. -->
    </style>

    <!-- Splash Screen theme -->
    <style name="Theme.SplashScreen" parent="Theme.AppCompat.Light.NoActionBar">

        <item name="android:windowBackground">@drawable/splash_screen</item>
    </style>
</resources>